
    <!-- Footer / Panneau de Commande -->
    <footer>
        <div class="container-fluid">
            <div class="d-flex justify-content-between align-items-center">
                <!-- Buttons -->
                <div class="commands">
                    <a href="{{ path('app_battle_log') }}" class="btn btn-dark">Journal</a>
                    <a href="{{ path('app_map', {'dimension': app.user.selectedEmpire.dimension}) }}" class="btn btn-dark">Carte</a>
                    <a href="{{ path('app_ranked') }}" class="btn btn-dark">Classement</a>
                    <a href="{{ path('app_forum') }}" class="btn btn-dark">Forum</a>
                    <a href="#" class="btn btn-dark">Diplomatie</a>
                </div>
                {#
                    <!-- Indicateurs --
                    <div class="indicators">
                        Défense: <span>80</span> | Attaque: <span>70</span> | Technologie: <span>60</span>
                    </div>

                    <!-- Actions rapides --
                    <div class="quick-actions">
                        <a href="#" class="btn btn-danger">Envoyer troupes</a>
                        <a href="#" class="btn btn-danger">Défendre</a>
                        <a href="#" class="btn btn-danger">Envoyer ressources</a>
                    </div>
                #}
            </div>
        </div>
    </footer>